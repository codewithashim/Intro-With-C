#include <stdio.h>

int main()
{
    int a = 3, b = 20;
    int sum = a + b;
    int mns = a - b;
    int mlt = a * b;
    int dev = a / b;
    int mod = b % a;

    // printf("%d" ,sum);
    // printf("%d" ,mns);
    // printf("%d" ,mlt);
    // printf("%d" ,dev);
    // printf("%d" ,mod);

    /**
     * < less then
     * <= less then equal
     *  > gether then
     * >= gether then equal
     * ==  equal
     * != not equal
     * &&
     * ||
     */

    // oparetar
    /**
     *  + , - , * , / , %
     */


    return 0;
}
